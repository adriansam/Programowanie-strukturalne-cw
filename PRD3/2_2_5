#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int podnies(int n);
int main()
{
    int n=0;
    printf("Podaj n: ");
    scanf("%d", &n);
    int result = podnies(n);
    printf("%d", result); //printf("%d", podnies(n);
    return 0;
}
int podnies(int n)
{
    return pow(2,n);
}
